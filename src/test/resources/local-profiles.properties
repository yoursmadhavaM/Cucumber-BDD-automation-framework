# Local profiles only - automation uses these when it starts.
# Do not use production credentials. Change values for your local test users.

# Default login profile (used for login scenarios)
default.username=testuser
default.password=testpass123

# Signup profile (used for create account scenarios)
signup.username=newuser
signup.email=newuser@test.local
signup.password=newpass123
